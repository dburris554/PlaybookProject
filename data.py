import pandas as pd
import numpy as np 

# define DataFrame
df = pd.DataFrame(columns=['Missile Count', 'Formation Used', 'Turrent Count','Missiles Survive'],dtype=int)
# What more data do we want?
# How to pull results from colloseum tests?  from colosseum import 'missile count variable'
# Will do more once I can figure out lines 6 and 7.
# I think collecting data from colosseum.py as series might work best, but not sure at the moment.